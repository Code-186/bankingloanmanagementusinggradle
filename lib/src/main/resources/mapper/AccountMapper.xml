<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crimsonlogic.bankingloanmanagementsystem.dao.AccountMapper">

    <resultMap id="AccountResultMap" type="com.crimsonlogic.bankingloanmanagementsystem.abstractclasses.Account">
        <id property="accountNumber" column="account_number" />
        <result property="customerId" column="customer_id" />
        <result property="accountType" column="account_type" />
        <result property="balance" column="balance" />
        <result property="bankName" column="bank_name" />
        <result property="branchId" column="branch_id" />
        <result property="status" column="status" />
        <result property="createdAt" column="created_at" />

        <discriminator javaType="java.lang.String" column="account_type">
            <case value="SAVINGS" resultType="com.crimsonlogic.bankingloanmanagementsystem.accountimplementation.SavingsAccount" />
            <case value="CURRENT" resultType="com.crimsonlogic.bankingloanmanagementsystem.accountimplementation.CurrentAccount" />
        </discriminator>
    </resultMap>

    <select id="findByAccountNumber" resultMap="AccountResultMap">
        SELECT account_number, customer_id, account_type, balance, bank_name, branch_id, status, created_at
        FROM accounts
        WHERE account_number = #{accountNumber}
    </select>

    <select id="findAccountsByCustomerId" resultMap="AccountResultMap">
        SELECT account_number, customer_id, account_type, balance, bank_name, branch_id, status, created_at
        FROM accounts
        WHERE customer_id = #{customerId}
    </select>

    <insert id="insertAccount" parameterType="com.crimsonlogic.bankingloanmanagementsystem.abstractclasses.Account">
        INSERT INTO accounts (account_number, customer_id, account_type, balance, bank_name, branch_id, status, created_at)
        VALUES (#{accountNumber}, #{customerId}, #{accountType}, #{balance}, #{bankName}, #{branchId}, #{status}, #{createdAt})
    </insert>

    <update id="updateBalance">
        UPDATE accounts
        SET balance = #{balance}
        WHERE account_number = #{accountNumber}
    </update>

    <update id="updateAccountStatus">
        UPDATE accounts
        SET status = #{status}
        WHERE account_number = #{accountNumber}
    </update>

</mapper>