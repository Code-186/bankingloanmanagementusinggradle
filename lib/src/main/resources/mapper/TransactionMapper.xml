<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crimsonlogic.bankingloanmanagementsystem.dao.TransactionMapper">

    <resultMap id="TransactionResultMap" type="com.crimsonlogic.bankingloanmanagementsystem.model.Transaction">
        <id property="transactionId" column="transaction_id"/>
        <result property="accountNumber" column="account_number"/>
        <result property="transactionType" column="transaction_type"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="transactionDate" column="transaction_date"/>
    </resultMap>

    <insert id="insertTransaction">
        INSERT INTO transactions (transaction_id, account_number, transaction_type, amount, status, transaction_date)
        VALUES (#{transactionId}, #{accountNumber}, #{transactionType}, #{amount}, #{status}, #{transactionDate})
    </insert>

    <select id="findTransactionsByAccountNumber" resultMap="TransactionResultMap">
        SELECT * FROM transactions WHERE account_number = #{accountNumber} ORDER BY transaction_date DESC
    </select>

    <select id="findAllTransactions" resultMap="TransactionResultMap">
        SELECT * FROM transactions ORDER BY transaction_date DESC
    </select>

    <select id="findLatestTransaction" resultMap="TransactionResultMap">
        SELECT * FROM transactions ORDER BY transaction_date DESC LIMIT 1
    </select>
</mapper>