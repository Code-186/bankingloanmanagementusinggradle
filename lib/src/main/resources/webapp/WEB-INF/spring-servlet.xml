<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation=" http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd
http://www.springframework.org/schema/mvc
http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Add support for component scanning (it scans all controllers) which
        are defined using @Controller annotation -->
    <context:component-scan
            base-package="com.crimsonlogic.springmvcusinggradle.controller" />
    <!-- Add support for conversion, formatting and validation support -->
    <mvc:annotation-driven />
    <!-- Define Spring MVC view resolver -->
    <bean
            class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>
    <!-- 1. Define the DataSource -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName"
                  value="com.mysql.cj.jdbc.Driver" />
        <property name="url"
                  value="jdbc:mysql://localhost:3306/springmvcmybatisexample" />
        <property name="username" value="root" />
        <property name="password" value="Aishu@215" />
    </bean>

    <!-- 2. Define the SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="mapperLocations" value="classpath:mappers/*.xml" />
    </bean>
    <!-- 3. Scan for Mapper Interfaces -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.crimsonlogic.springmvcusinggradle.dao" />
        <property name="sqlSessionFactory" ref="sqlSessionFactory" />
    </bean>

    <bean id="employeeService"
          class="com.crimsonlogic.springmvcusinggradle.service.EmployeeService">

        <property name="employeeMapper"
                  ref="employeeMapper"/>

    </bean>

    <bean id="departmentService"
          class="com.crimsonlogic.springmvcusinggradle.service.DepartmentService">

        <property name="departmentMapper"
                  ref="departmentMapper"/>

    </bean>

    <bean id="branchService"
          class="com.crimsonlogic.springmvcusinggradle.service.BranchService">

        <property name="branchMapper"
                  ref="branchMapper"/>

    </bean>

</beans>